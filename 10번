모듈화 프로그램 Modular Program
  - 구조화된 프로그램(Structure Program)
    - 함수를 이용해 잘 구성된 프로그램
    
Bitwise operator의 '^'를 사용 시 저장할 local valiable 선언 없이도 swap 구현이 가능하다
  ex) x = x^y
      y = x^y
      x = x^y
      
Function Pointer
  - 포인터를 이용해 함수를 간접적으로 호출
  - 프로그램의 유연성 증대
  
구조체 Structure
  - 서로 밀접한 연관이 있는 변수들을 모아놓은 복합변수
  - 사용자 정의형 user defined type
    - 나중에 객체라고 지칭한다

구조체 포인터 Struct Pointer
  - 외부 함수에서 사용 시 structure.value 가 아니라
    structure->value 로 사용해야 한다

Field
  - 구조체 내부에 선언된 value들을 각각의 field라고 한다
* pointer로 heap 영역에 dynamic allocate한 것들은 garbage 관리를 위해 free함수 필수

atoi
  - ASCII to Integer 아스키 코드를 정수로 변환
  
포인터 Pointer
  - 메모리 주소(address)를 나타내는 기호화된 표현
  
난수 Random Number

통용범위 Scope

기억 영역 분류 Storage Class
  1. automatic variable - 자동 변수
  2. static variable - 정적 변수
  3. external variable - 외부 변수
  4. register variable - 레지스터 변수(사용 방법이 뭘까)
  
선형 탐색 Linear Search
  - sorting 되어 있지 않은 데이터에서 찾을 때 사용
  - 시간 복잡도 : O(n) - 데이터가 많을 수록 오래 걸림
  
이진 탐색 Binary Search
  - sorting 되어 있는 데이터에서 찾을 때 사용
  - 시간 복잡도 : O(log(n)) - 선형 탐색보다 빠르다
